# Copy to keystore.properties (gitignored) for local release builds.
# Never commit keystore.properties or your .jks / .keystore file.

storeFile=nofy-upload.keystore
storePassword=change-me
keyAlias=upload
keyPassword=change-me
